rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMERGENCY DEBUGGING RULES - EXTREMELY PERMISSIVE
    // Allow any authenticated user to read/write anything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}