rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: More permissive rules for testing multiple students
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}